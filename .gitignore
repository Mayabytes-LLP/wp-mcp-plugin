# OS noise
.DS_Store
Thumbs.db
desktop.ini
ehthumbs.db

# Editor / IDE
*.swp
*.swo
*~
.idea/
.vscode/*
!.vscode/settings.json
.cursor/*
!.cursor/settings.json
*.sublime-workspace

# Dev/test artifacts
*.log
phpunit.xml
.phpunit.result.cache

# vendor/ is intentionally committed — this is a drop-in plugin and
# end users must not need Composer. Do NOT add vendor/ here.
composer.phar

# node_modules/ is intentionally committed — this is a drop-in plugin and
# end users must not need Node.js. Do NOT add node_modules/ here.
node_modules/

# Environment variables
.env.*

opencode.jsonc